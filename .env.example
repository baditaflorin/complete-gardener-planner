# Complete Gardener Planner is static-first and needs no runtime secrets.
# Keep real API keys in a local .env file only when adding offline data generators.
PUBLIC_WEATHER_CACHE_SOURCE=
PUBLIC_MODEL_RELEASE_TAG=v0.1.0
